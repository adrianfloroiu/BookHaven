@{
    ViewData["Title"] = "Access Denied";
}

<div class="d-flex justify-content-center align-items-center" style="height: calc(100vh - 150px);">
    <div class="card p-5 shadow text-center w-75" style="max-width: 40rem;">
        <h2 class="text-danger fw-bold">Access Denied</h2>
        <p class="mt-3">You do not have permission to access this page.</p>
        <i class="bi bi-exclamation-triangle-fill text-warning display-3"></i>
        <div class="mt-4">
            <a href="@Url.Action("Index", "Home")" class="btn btn-primary">To Home Page</a>
        </div>
    </div>
</div>
